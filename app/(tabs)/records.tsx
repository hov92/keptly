import { Text } from 'react-native';
import { Screen } from '../../components/screen';

export default function RecordsScreen() {
  return (
    <Screen>
      <Text style={{ fontSize: 28, fontWeight: '700' }}>Records</Text>
    </Screen>
  );
}